receivers:
  hostmetrics:
    collection_interval: 15s
    scrapers:
      cpu:
      memory:
      disk:
      network:

exporters:
  otlp:
    endpoint: "{target hostname}:{target port}"

service:
  pipelines:
    metrics:
      receivers: [hostmetrics]
      exporters: [otlp]